import Link from "next/link";

export function AdminSectionPage({ title, description, links = [], rows = [] }: { title: string; description: string; links?: { href: string; label: string }[]; rows?: string[][] }) {
  return <main className="mx-auto max-w-7xl"><div className="mb-8"><p className="text-xs font-bold uppercase tracking-[0.24em] text-[#0d2946]">Admin Platform</p><h1 className="mt-3 text-4xl font-black tracking-[-0.04em]">{title}</h1><p className="mt-3 max-w-2xl text-slate-600">{description}</p></div>{links.length > 0 && <div className="mb-6 flex flex-wrap gap-3">{links.map((link) => <Link key={link.href} href={link.href} className="rounded-full bg-[#0d2946] px-4 py-2 text-sm font-bold text-white">{link.label}</Link>)}</div>}<div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm">{rows.length > 0 ? <table className="min-w-full text-left text-sm"><tbody>{rows.map((row, index) => <tr key={index} className="border-t border-slate-200 first:border-t-0">{row.map((cell, cellIndex) => <td key={cellIndex} className={`px-5 py-4 ${cellIndex === 0 ? "font-bold text-[#0d2946]" : "text-slate-700"}`}>{cell}</td>)}</tr>)}</tbody></table> : <div className="p-8 text-sm text-slate-500">No records are available yet. New website submissions will appear here when stored.</div>}</div></main>;
}
